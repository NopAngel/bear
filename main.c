#include <stdio.h>
#include "include/INFO.h"

int main()
{
    printf(VERSION);
    printf(REPO);
    printf(AUTHOR);
    printf(KERNEL_V);
    printf(OS);
    printf(Shell_V);
    return 0;
}
