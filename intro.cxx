#include <iostream>

int main(){
    std::cout << "Hello, everyone!!";
    std::cout << "This is the tutorial. It will explain how to build BearOS.";
    std::cout << "1) Go to the file called: Makefile (obviously you already have the repository cloned)";
    std::cout << "2) Type the following commands:
                    make iso

                    # You should already have Make installed";
    std::cout << "3) Wait for the kernel to be built";
    std::cout << "And that's it, you now have your .iso built!";

    return 0;
}